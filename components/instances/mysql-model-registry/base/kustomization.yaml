apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rhoai-model-registries

resources:
- mysql-secret.yaml
# PVC excluded - already exists and bound
# - mysql-pvc.yaml
- mysql-deployment.yaml
- mysql-service.yaml
- mysql-auth-fix.yaml

# Ne pas redéfinir commonLabels ici pour éviter les conflits
# Les labels seront appliqués par le kustomization parent
