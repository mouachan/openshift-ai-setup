apiVersion: v1
kind: ConfigMap
metadata:
  name: elyra-runtime-config
  namespace: triton-demo
  labels:
    app.kubernetes.io/name: elyra-runtime-config
    app.kubernetes.io/part-of: triton-demo
data:
  # Script d'initialisation du runtime basé sur le script officiel OpenShift AI
  init-runtime.py: |
    #!/usr/bin/env python3
    """
    Script pour configurer automatiquement le runtime Elyra
    Basé sur la méthode officielle OpenShift AI
    """
    import os
    import json
    from pathlib import Path

    # Configuration basée sur le modèle Data Science Project
    ELYRA_METADATA_DIR = os.path.expanduser("~/.local/share/jupyter/metadata")
    NAMESPACE = "triton-demo"
    CLUSTER_DOMAIN = "apps.cluster-v2mx6.v2mx6.sandbox1062.opentlc.com"

    def ensure_elyra_dirs():
        """Crée les répertoires Elyra nécessaires"""
        dirs = [
            f"{ELYRA_METADATA_DIR}/runtimes",
            f"{ELYRA_METADATA_DIR}/component-catalogs",
            f"{ELYRA_METADATA_DIR}/code-snippets",
        ]
        
        for dir_path in dirs:
            Path(dir_path).mkdir(parents=True, exist_ok=True)
            print(f"✓ Répertoire créé: {dir_path}")

    def create_kubeflow_runtime():
        """Crée la configuration runtime Kubeflow Pipelines comme OpenShift AI"""
        return {
            "display_name": "Data Science Pipelines",
            "metadata": {
                "api_endpoint": f"https://ds-pipeline-{NAMESPACE}-pipelines-{NAMESPACE}.{CLUSTER_DOMAIN}",
                "api_username": "",
                "api_password": "",
                "cos_endpoint": "http://minio-api.minio.svc:9000",
                "cos_username": "minioadmin",
                "cos_password": "minioadmin",
                "cos_bucket": "triton-data",
                "cos_directory": "",
                "tags": ["kubeflow", "pipelines", "openshift"],
                "engine": "Argo",  # Corrigé : Argo au lieu de Kubeflow Pipelines
                "auth_type": "NO_AUTHENTICATION",
                "runtime_type": "KUBEFLOW_PIPELINES",
                "api_version": "v1",
                "user_namespace": NAMESPACE,
                "engine_namespace": NAMESPACE,
                "cos_secure": False,
                "disable_ssl_verification": False,
                "cos_auth_type": "USER_CREDENTIALS"
            },
            "schema_name": "kfp",
            "name": "data_science_pipelines"
        }

    def save_metadata(metadata, file_path):
        """Sauvegarde les métadonnées dans un fichier JSON"""
        try:
            os.makedirs(os.path.dirname(file_path), exist_ok=True)
            with open(file_path, 'w', encoding='utf-8') as f:
                json.dump(metadata, f, indent=2, ensure_ascii=False)
            print(f"✓ Runtime configuré: {file_path}")
            return True
        except Exception as e:
            print(f"✗ Erreur lors de la sauvegarde {file_path}: {e}")
            return False

    def main():
        """Configuration principale du runtime comme OpenShift AI"""
        print("🔧 Configuration automatique du runtime Elyra (méthode OpenShift AI)")
        print("=" * 70)
        
        # Créer les répertoires nécessaires
        ensure_elyra_dirs()
        
        # Créer le runtime Kubeflow comme OpenShift AI
        runtime_config = create_kubeflow_runtime()
        
        # Sauvegarder dans le bon répertoire
        runtime_file = f"{ELYRA_METADATA_DIR}/runtimes/data_science_pipelines.json"
        
        if save_metadata(runtime_config, runtime_file):
            print("\n✅ Configuration terminée avec succès!")
            print(f"📁 Fichier: {runtime_file}")
            print(f"🔗 Endpoint: {runtime_config['metadata']['api_endpoint']}")
            print(f"📦 Bucket S3: {runtime_config['metadata']['cos_bucket']}")
            print(f"🏷️ Nom du runtime: {runtime_config['display_name']}")
            print(f"⚙️ Engine: {runtime_config['metadata']['engine']}")
            
            # Vérifier que le fichier existe
            if os.path.exists(runtime_file):
                print("✅ Le runtime est prêt à être utilisé dans Elyra!")
            else:
                print("❌ Erreur: Le fichier de runtime n'a pas été créé")
        else:
            print("❌ Échec de la configuration du runtime")

    if __name__ == "__main__":
        main()

  # Configuration Elyra avancée
  jupyter_elyra_config.py: |
    # Configuration Elyra pour les pipelines
    c.ElyraApp.enable_pipeline_editing = True
    c.ElyraApp.runtime_env = 'kubeflow_pipelines'
    
    # Configuration des data connections
    c.ElyraApp.data_connections = {
        'triton-data-connection': {
            'type': 's3',
            'endpoint': 'http://minio-api.minio.svc:9000',
            'access_key': 'minioadmin',
            'secret_key': 'minioadmin',
            'bucket': 'triton-data'
        }
    } 